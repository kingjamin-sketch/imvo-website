import type { ReactNode } from "react";
import "../applied/applied.css";
import "../applied/applied-refresh.css";

export default function SystemsLayout({ children }: { children: ReactNode }) {
  return children;
}
